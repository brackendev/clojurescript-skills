interface:
  display_name: "Upgrade ClojureScript"
  short_description: "Upgrade the ClojureScript dependency in deps.edn to the latest released version."
  default_prompt: "Upgrade ClojureScript to the latest version."
  brand_color: "#F7DF1E"

policy:
  allow_implicit_invocation: false
