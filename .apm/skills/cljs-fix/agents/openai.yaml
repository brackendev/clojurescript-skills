interface:
  display_name: "ClojureScript Fix"
  short_description: "Fix a ClojureScript project (lint, format, test, advanced, dry). The format step writes by default; --report swaps it for cljfmt check."
  default_prompt: "Fix this ClojureScript project."
  brand_color: "#F7DF1E"

policy:
  allow_implicit_invocation: false
